@model List<DegreeMapping.Models.Course>



@foreach (DegreeMapping.Models.Course c in Model.OrderBy(x => x.Code))
{
    using (Html.BeginForm("CodeSave", "App", FormMethod.Post, new { @Class = "form-horizontal" }))
    {
        @Html.Action("_CodeEdit", new { c = c })
    }
}