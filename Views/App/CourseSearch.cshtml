@model  List<DegreeMapping.Models.Course>


<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Search Course</h1>
        </div>
    </div>

    <div class="row py-5">
        <div class="col-md-12">
            @using (Html.BeginForm("CourseSearch", "App", FormMethod.Post, new { @Class = "form-inline" }))
            {
                
                <div class="form-group">
                    <input type="text" class="form-control" name="keyword" value="@ViewBag.Keyword"/>
                    <button type="submit" name="submit" class="btn btn-primary btn-lg">Search</button>
                </div>
            }
        </div>
    </div>

    @if (Model.Count > 0)
    {
        int count = 0;
        <div class="alert alert-warning">Found @Model.Count courses</div>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>&nbsp;</th>
                    <th>Code</th>
                    <th>Degree</th>
                    <th>Institution</th>
                    <th>&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                @foreach (DegreeMapping.Models.Course c in Model.OrderBy(x => x.Code).ThenBy(x => x.Degree).ThenBy(x => x.Institution))
                {
                    count++;
                    <tr>
                        <td>@count</td>
                        <td>@c.Code</td>
                        <td>@c.Degree</td>
                        <td>@c.Institution</td>
                        <td>@Html.ActionLink("Edit", "CourseEdit", new { id = c.Id }, new { @class = "btn btn-primary btn-lg" })</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>